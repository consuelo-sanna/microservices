//import ListingsService from '#root/adapters/ListingsService';

const listingsResolver = async () => {
    [{ description: 'oaiwdjoaiwd', id: 2, title: 'mock' }];
};

export default listingsResolver;
